
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/estacionamento?useTimezone=true&serverTimezone=UTC&useLegacyDate
    username: postgres
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate.hibernate.format_sql: true
  springdoc:
    api-docs:
      enabled: true
    swagger-ui:
      enabled: true